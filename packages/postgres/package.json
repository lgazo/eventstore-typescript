{
  "name": "@ricofritzsche/eventstore-postgres",
  "version": "1.0.8",
  "description": "PostgreSQL-backed store for @ricofritzsche/eventstore",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist",
    "prepack": "npm run build",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "eventstore",
    "event-sourcing",
    "postgres",
    "postgresql",
    "event-driven"
  ],
  "author": "Rico Fritzsche",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ricofritzsche/eventstore-typescript"
  },
  "bugs": {
    "url": "https://github.com/ricofritzsche/eventstore-typescript/issues"
  },
  "homepage": "https://github.com/ricofritzsche/eventstore-typescript#readme",
  "peerDependencies": {
    "@ricofritzsche/eventstore": "^1.0.8"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.0"
  },
  "engines": {
    "node": ">=18.19.0"
  }
}
